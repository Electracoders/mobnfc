QT       -= gui

TARGET = snep-encode
CONFIG   += console
CONFIG   -= app_bundle

TEMPLATE = app

INCLUDEPATH += ../../extras/libndef/include

win32: {
	    LIBS += -L../libndef/release/
	    LIBS += -lndef1
}

unix: {
	    # Link to the library generated by the project.  Could use variables or
	    # something here to make it more bulletproof
	    LIBS += ../../extras/libndef/libndef/libndef.so
	    # Specify that we depend on the library (which, logically would be implicit from
	    # the fact that we are linking to it)
	    PRE_TARGETDEPS += ../../extras/libndef/libndef/libndef.so
}

SOURCES += snep-encode.cpp

unix: {
    # install binairies
    isEmpty(PREFIX) {
      PREFIX = /usr/local
    }
    target.path = $$PREFIX/bin
    INSTALLS += target
}
